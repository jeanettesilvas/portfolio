@AGENTS.md

# Git workflow

- Always create a feature branch before committing. Branch off `main` using a short kebab-case name describing the change (e.g. `dark-mode`, `fix-nav-links`).
- Never commit directly to `main`.
- After committing, push the branch and open a PR against `main`.
